# Використовуємо базовий образ Python
FROM python:3.12

# Встановлюємо робочу директорію всередині контейнера
WORKDIR /app

# Копіюємо файли вашого проекту до робочої директорії
COPY . /app

# Вказуємо, що запускатиметься в контейнері
CMD ["python3", "assistant.py"]