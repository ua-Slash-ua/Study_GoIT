# Вказуємо базовий образ
FROM python:3.12-slim

# Встановлюємо робочу директорію
WORKDIR /app

# Копіюємо всі файли з локальної директорії в контейнер
COPY . .

# Встановлюємо логіку для запуску сервера
CMD ["python", "main.py"]